import javax.swing.*;
import java.awt.*;

public class HistoryPanel extends JPanel {
    public HistoryPanel(Dimension size) {
        this.setPreferredSize(new Dimension(size));
        this.setBackground(new Color(29, 27, 26));
    }
}
