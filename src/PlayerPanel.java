import javax.swing.*;
import java.awt.*;

public class PlayerPanel extends JPanel {
    public PlayerPanel(Dimension tabSize) {
        this.setPreferredSize(tabSize);
        this.setBackground(new Color(49, 46, 43));
    }
}
